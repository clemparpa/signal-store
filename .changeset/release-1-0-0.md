---
"@fluch/signal-store": major
"@fluch/signal-store-entities": major
"@fluch/signal-store-react": major
"@fluch/signal-store-devtools": major
---

Initial stable release at `1.0.0`. All four public packages now share a unified version line via changeset `fixed` mode — a bump on any package bumps the whole group. This locks the API surface for v1 and signals semver compatibility going forward.
