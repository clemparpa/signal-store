# @flush/signal-store-react

React Provider + `useStore` hook for `@flush/signal-store` (option B: scoped store instances per subtree).

> Status: scaffolding only. API not yet implemented. See [`spec.md`](../../spec.md) at the repo root.
