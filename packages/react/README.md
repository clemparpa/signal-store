# @fluch/signal-store-react

React Provider + `useStore` hook for `@fluch/signal-store` (option B: scoped store instances per subtree).

> Status: scaffolding only. API not yet implemented. See [`spec.md`](../../spec.md) at the repo root.
