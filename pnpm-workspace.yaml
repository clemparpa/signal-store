packages:
  - 'packages/*'

# Allow these packages to run their post-install build scripts.
# Both ship platform-specific native binaries that need to be installed.
allowBuilds:
  '@biomejs/biome': true
  esbuild: true
